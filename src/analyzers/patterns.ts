/**
 * Pattern detection (sequences).
 *
 * Exact-match counting and sliding-window sequence detection (2–5 commands)
 * are already implemented in {@link analyzeFrequency} within `frequency.ts`.
 *
 * This module is intentionally empty. If complementary logic is needed later
 * (e.g. prefix-tree patterns, regex-based grouping), it should go here
 * rather than duplicating what `frequency.ts` already provides.
 */
export {};
